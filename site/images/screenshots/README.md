# Landing screenshots

CSS mocks show until real GIFs are present.

Drop files here (exact names):

- `01-menu-bar.gif`
- `02-settings-accounts.gif`
- `03-folder-rules.gif`

Then commit and deploy Pages. The carousel auto-hides the mock when a GIF loads.
